name: Hello World

on:
  workflow_dispatch:

jobs:
  say-hello-inline-bash:
    runs-on: ubuntu-24.04 # Runs on github-hosted Ubuntu 24.04 runners
    name: Say Hello from an Inline Bash Script
    steps:
      - run: echo "Hello from an inline bash script in a GitHub Action Workflow!"